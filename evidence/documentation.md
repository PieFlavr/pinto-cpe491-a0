# Documentation trail

Use the two official ROS 2 Jazzy source files linked in Part 3. Quote only the short line or fragment needed to identify each element.

NOTE: I was unsure if this was pertaining to a particular file, or was generic, so treat any FULLY CAPTITALIZED word or set of words as generic "variable" names to substitute for the appropriate purpose/application.

NOTE #2: Additional to the above, in-code comments will be used as substitute for "specific" documentation, as to avoid the issue of multiple individual independently updatable code bits.

| Code element | File and line or short fragment | What this element does |
|---|---|---|
| Import the message type | `from std_msgs.msg import String` | [brief note] |
| Assign the node name | `super().__init__('NODE_NAME')`` | [brief note] |
| Create the publisher | `PUBLISHER = StatusPublisher()` | [brief note] |
| Create the subscription | [Your answer] | [brief note] |
| Assign the topic name | [Your answer] | [brief note] |
| Control when a callback occurs | [Your answer] | [brief note] |
| Initialize, run, and shut down ROS | [Your answer] | [brief note] |

**One place where the documentation helped me correct or avoid a mistake:**  
[Your answer]
